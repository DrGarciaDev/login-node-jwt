# login-node-jwt App simple para login con Node js y JsonWebToken

Correr los comandos ``` npm i express jsonwebtoken --save ``` para instalar las librerías necesarias

Es preciso tener instalado nodemom o instalar con ``` npm i -g nodemon ```
